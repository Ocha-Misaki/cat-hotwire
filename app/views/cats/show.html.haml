%div{id: "#{dom_id @cat}"}
  %p
    %strong 名前:
    = @cat.name
  %p
    %strong 年齢:
    = @cat.age
%div
  = link_to "編集", edit_cat_path(@cat)
  = link_to "戻る", cats_path
  = button_to "削除", @cat, method: :delete, class: "btn btn-danger"
